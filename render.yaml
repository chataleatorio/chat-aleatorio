services:
  - type: web
    name: video-chat-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ACCESS_PASSWORD
        value: chat1999
      - key: SESSION_SECRET
        generateValue: true
